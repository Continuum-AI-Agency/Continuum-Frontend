// Shared height cap for the side-by-side dashboard panels (Insights beside Top
// creatives / Top campaigns). ~4.5 insight rows peek so the partial last row
// signals "scroll for more"; the equal cap keeps the two columns balanced.
export const DASHBOARD_PANEL_MAX_HEIGHT = '21rem';
