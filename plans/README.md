# Frontend Implementation Plans

Generated by the `/improve` skill on 2026-06-17 (focus: Continuum-Frontend). Each plan is self-contained for a zero-context executor. Read the plan fully before starting, honor its STOP conditions, and update your row when done.

**Planned against commit `c07c0c6`** (working tree was dirty at that commit; verify "Current state" excerpts against live code before editing). Baseline at authoring: `bunx tsc --noEmit` → 0 errors; test runner = `bun test` (happy-dom preload via `bun-test-setup.ts`); **no CI existed**.

## Execution order & status

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 001 | Add CI pipeline (GitHub Actions) | P1 | S | LOW | — | TODO |
| 002 | Remove dead deps + align eslint-config-next | P1 | S | LOW | — | TODO |
| 003 | Remove prohibited `*_ENABLED` env gates | P1 | S | LOW | — | TODO |
| 004 | Fix StudioCanvas stale-closure error reporting | P1 | S | LOW | — | TODO |
| 005 | Fix OrganicAgentPanel debounce unmount leak | P1 | S | LOW | — | TODO |
| 006 | Extract shared format module | P2 | M | LOW | — | TODO |
| 007 | RSC-seed first-paint organic dashboard | P2 | M | MED | — | TODO |
| 008 | Migrate Jaina stream schemas → contracts | P2 | L | MED | 001 | TODO |
| 009 | Consolidate analytics + charting libs (spike) | P3 | M | MED | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale).

## Dependency notes

- No plan hard-blocks another, but **001 (CI) should land first** so every later change runs against a green gate.
- **008** (large) should run only on green CI (depends on 001).
- **009** is a design/spike — no production code until data-owner sign-off.

## Common commands

Run from `Continuum-Frontend/` unless noted:
- Typecheck: `bunx tsc --noEmit` → exit 0
- Tests: `bun test [path]` → all pass
- Lint: `bun run lint` → exit 0
- Install (monorepo root): `bun install`
- Build: `bun run build`

## Findings considered and rejected

- **Committed `package-lock.json`**: rejected — `git ls-files` shows it is untracked (on-disk only), not a finding.

## Deferred findings (audited, not planned this round)

Available for a future `/improve` run. **Security:** SEC-01 unauthenticated `/api/events` SSE firehose + default-open ingest token (`src/app/api/events/route.ts:73,114`); SEC-03 SSRF in `src/app/api/organic/ai-studio/apply/route.ts:150`; SEC-05 missing CSP/security headers (`next.config.ts` has no `headers()`); SEC-04 vulnerable `dompurify@2.5.9` on the markdown/PDF path; SEC-02/SEC-06 IDOR/missing brand-ownership checks on paid-media `campaign-indexes`/`product-catalogs`/insights routes. **Tests:** route handlers (12/68 tested), Server Actions (0/5), `src/lib/api/http.ts` `request()`. **Tech-debt/DX:** ARCH-05 god-file splits, ARCH-07 `as any` clusters (StudioCanvas/Realtime + missing `canvas_*` Supabase types), DX-02 prettier/format setup, DX-03 logger boundary (354 `console.*`), DX-04 single test runner, PERF-02 list virtualization; stream-lifecycle bugs BUG-03/06/07/08/09/11.

## Direction options (separate from defects)

- **DIR-01** — ship the approved-but-unbuilt AI Studio Conversational Canvas Agent (`ROADMAP.md` "Approved plan, not started"; target dirs don't exist).
- **DIR-02** — close the organic publish gap (planner generates but can't post; `docs/organic-current-capabilities.md`).
- **DIR-03** — dedupe `ROADMAP.md`/`Roadmap.md` (byte-identical, case-twin hazard).
