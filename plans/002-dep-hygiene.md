# Plan 002: Dead deps removed and eslint-config-next matches Next 16

> **Executor instructions**: Follow this plan step by step. Run every verification command and confirm the expected result before moving on. If a STOP condition occurs, stop and report — do not improvise. When done, update this plan's row in `plans/README.md`.
>
> **Drift check (run first)**: `git -C . status Continuum-Frontend/package.json` and re-run the three "dead" greps in Step 1. If `package.json` already changed these lines, compare against "Current state" before editing. Working tree was dirty at the planned commit.

## Status

- **Priority**: P1
- **Effort**: S
- **Risk**: LOW
- **Depends on**: none
- **Category**: deps
- **Planned at**: commit `c07c0c6`, 2026-06-17

## Why this matters

`@tanstack/react-virtual`, `maath`, and the placeholder `radix@^0.0.0` have zero import sites — they bloat installs, drift the manifest, and mislead contributors (react-virtual implies virtualization that doesn't exist anywhere). Separately, `eslint-config-next` is pinned to `15.5.4` while `next` is `^16.2.6`; lint is the only static gate beyond `tsc`, and it currently runs a major-version-stale ruleset that misses Next 16 / React 19 rules.

## Current state

In `Continuum-Frontend/package.json`:
- `"@tanstack/react-virtual": "^3.13.21"` (dependencies)
- `"maath": "^0.10.8"` (dependencies)
- `"radix": "^0.0.0"` (devDependencies) — a bogus placeholder; the real package is `radix-ui` (127 uses — KEEP it)
- `"eslint-config-next": "15.5.4"` (devDependencies)
- `"next": "^16.2.6"` (dependencies)

Verified zero usage:
- `grep -rnE "react-virtual|useVirtualizer|Virtualizer" src` → 0
- `grep -rn "\bmaath\b" src` → 0
- `grep -rn 'from "radix"' src` → 0

## Commands you will need

| Purpose | Command | Expected |
|---------|---------|----------|
| Confirm dead | the three greps above (from `Continuum-Frontend/`) | 0 each |
| Install | `bun install` (monorepo root) | exit 0, updates `bun.lock` |
| Typecheck | `bunx tsc --noEmit` (from `Continuum-Frontend/`) | exit 0 |
| Lint | `bun run lint` (from `Continuum-Frontend/`) | exit 0 |

## Scope

**In scope**: `Continuum-Frontend/package.json`, root `bun.lock` (regenerated by `bun install`).

**Out of scope** (do NOT touch):
- `radix-ui` (the real, heavily-used package).
- `zustand` / `jotai` / `@tanstack/react-query` (state-lib consolidation is a separate concern).
- Re-adding virtualization — that is a deliberate future task, not this one.

## Git workflow

- Branch: `advisor/002-dep-hygiene`
- Two logical commits: (1) `chore(deps): remove unused react-virtual, maath, radix placeholder`; (2) `chore(deps): bump eslint-config-next to ^16`.
- Do NOT push or open a PR unless instructed.

## Steps

### Step 1: Re-confirm the three deps are dead

Run the three greps from "Current state". **STOP** if any returns > 0 — the dep is actually used; report it (do not remove).

### Step 2: Remove the dead deps

Delete the `@tanstack/react-virtual`, `maath`, and `radix` lines from `package.json`. Run `bun install` (root). Commit (logical unit 1).

**Verify**: `bunx tsc --noEmit` exit 0; the three greps still 0; `git diff package.json` shows exactly three removed lines.

### Step 3: Align eslint-config-next, then lint

Change `"eslint-config-next": "15.5.4"` → `"eslint-config-next": "^16.2.6"`. Run `bun install` (root). Run `bun run lint`.

If lint now reports **errors** (not warnings), **STOP and report the full list** — do not mass-fix here; the user decides fix-now vs follow-up. Commit (logical unit 2) only if lint is clean.

**Verify**: `bun run lint` exit 0 (or the error list reported); `bunx tsc --noEmit` exit 0.

## Test plan

- No new tests (dependency change). The gate is `tsc` clean + `lint` clean + dead-greps zero + the existing `bun test` suite still green (`bun test`).

## Done criteria

- [ ] `bunx tsc --noEmit` exits 0.
- [ ] `grep` for `react-virtual`/`maath`/`from "radix"` in `src` returns 0.
- [ ] `eslint-config-next` is `^16.2.6`; `bun run lint` exits 0 (or errors reported and paused).
- [ ] `package.json` diff is minimal (3 removals + 1 version change); `bun.lock` updated.
- [ ] `plans/README.md` status row updated.

## STOP conditions

- Any dead-grep > 0 in Step 1.
- Lint reports errors after the eslint-config-next bump.
- `bun install` fails on a frozen lockfile / cannot resolve `eslint-config-next@^16`.

## Maintenance notes

- Removing `@tanstack/react-virtual` interacts with the deferred virtualization finding (PERF-02). If large-list virtualization is later wanted (library media grids, planner day-stacks), re-add the dep deliberately as part of that task.
- A reviewer should confirm no dynamic `import("react-virtual"|"maath")` exists (greps in Step 1 already cover this).
